# Spur Web App

TanStack Start frontend for the Spur AI live chat take-home.

Use the root workspace commands:

```sh
pnpm dev:web
pnpm build
pnpm --filter web test
```

Set `VITE_API_BASE_URL` to the deployed or local API URL.
