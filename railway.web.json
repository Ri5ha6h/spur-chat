{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm --filter @spur/shared build && pnpm --filter web build && rm -rf .output && mkdir -p .output && cp -R apps/web/.output/public .output/public",
    "watchPatterns": [
      "/apps/web/**",
      "/packages/shared/**",
      "/package.json",
      "/pnpm-lock.yaml",
      "/pnpm-workspace.yaml",
      "/.node-version",
      "/.npmrc"
    ]
  },
  "deploy": {
    "startCommand": null,
    "healthcheckPath": "/",
    "healthcheckTimeout": 30
  }
}
